using UnityEngine;

///  this will ensure that any class that implements this interface will have everything included in the interface
public interface IDamageable {

    void TakeDamage(float amount);

}
